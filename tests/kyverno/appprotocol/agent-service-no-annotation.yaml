apiVersion: v1
kind: Service
metadata:
  name: unannotated-agent
  namespace: tenant-alpha
  labels:
    app: kagent
  ownerReferences:
    - apiVersion: kagent.dev/v1alpha2
      kind: Agent
      name: unannotated-agent
      uid: test-uid-002
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: kagent
