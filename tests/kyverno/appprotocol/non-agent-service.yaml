apiVersion: v1
kind: Service
metadata:
  name: regular-service
  namespace: tenant-alpha
  labels:
    app: kagent
  annotations:
    platform.agentic.io/expose: "true"
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: kagent
