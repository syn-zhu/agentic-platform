apiVersion: v1
kind: Service
metadata:
  name: my-assistant
  namespace: tenant-alpha
  labels:
    app: kagent
  annotations:
    platform.agentic.io/expose: "true"
  ownerReferences:
    - apiVersion: kagent.dev/v1alpha2
      kind: Agent
      name: my-assistant
      uid: test-uid-001
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: kagent
